Drop portrait / real photography here (e.g. `lynn-hero.jpg`, `lynn-about.jpg`) and update the
`<img>` placeholders in `index.html` — the brand bible calls for full-bleed real photography,
which isn't something this build can generate. Until then, the layout falls back gracefully to
flat color blocks.
